# Wanna Understanding — 领域上下文

> 本文件定义项目内统一术语，供 Issue、测试与文档引用。

---

## 核心对象

| 术语 | 含义 |
|------|------|
| **目标窗口** | 用户指定的需要监控的应用程序窗口（如 VS Code、Cursor、记事本） |
| **监控区域** | 从目标窗口中裁剪出的用来做 OCR 的矩形区域（默认中间 70% 区域） |
| **代码快照** | 某时刻屏幕上可见代码的文本表示，附带 SHA256 哈希用于变化检测 |
| **分析结果** | AI 对代码快照的反馈，包含「功能说明」「潜在 Bug」「改进建议」三部分 |

## 屏幕与捕获

| 术语 | 含义 |
|------|------|
| **DPI 缩放** | Windows 高 DPI 显示下，截图坐标需要乘以缩放因子才能对应物理像素 |
| **活动窗口** | 当前拥有键盘焦点的窗口，通过 `win32gui.GetForegroundWindow()` 获取 |
| **窗口矩形** | 窗口在屏幕上的位置和尺寸（left, top, right, bottom） |
| **中间区域** | 从窗口中裁剪出中间 70% 高度 × 90% 宽度的区域，过滤行号和菜单栏 |

## OCR

| 术语 | 含义 |
|------|------|
| **预处理管线** | 应用在截图上的图像处理步骤链：放大 → 灰度 → 二值化 → 反色 |
| **文本行** | OCR 返回的单行识别结果，包含文本内容和边界框坐标 |
| **置信度** | OCR 对单行识别结果的置信分数（0-1），低于阈值时丢弃 |
| **后处理** | OCR 输出后对文本的修正步骤：去重相邻行、修复特殊符号、恢复缩进 |

## AI 分析

| 术语 | 含义 |
|------|------|
| **系统 Prompt** | 发给 AI 的角色设定指令，定义其行为边界和输出格式 |
| **结构输出** | AI 回复中要求格式化为固定结构（功能 / Bug / 建议），便于程序解析 |
| **代码哈希** | `SHA256(代码文本)` 的值，用作缓存键和变化检测依据 |
| **LRU 缓存** | 最近最少使用淘汰策略缓存 AI 分析结果，上限可配置 |
| **上下文裁剪** | `trim_to_context`：OCR 无法获知光标，取识别文本垂直中间 N 行发给 AI |
| **防抖窗口** | 滚动/切换停止后等待的时间窗口（默认 0.5s），期间不触发新分析 |

## UI

| 术语 | 含义 |
|------|------|
| **悬浮窗** | 无边框、置顶、半透明的展示窗口，显示分析结果 |
| **鼠标穿透** | 悬浮窗不拦截鼠标事件，点击穿透到下层窗口 |
| **流式输出** | AI 回复逐 token 传入悬浮窗，实现打字机效果，减少等待感知 |

## 工程术语

| 术语 | 含义 |
|------|------|
| **仓库根目录** | Git 克隆顶层 |
| **Python 包目录** | `src/wanna_understanding/` |
| **测试** | `tests/` 目录 |
| **全局配置** | `src/wanna_understanding/config.py` — API Key、截图频率、模型参数 |
| **定时轮询** | 每 N 秒检查一次目标窗口内容是否变化（当前默认 2s） |
| **事件触发** | 通过系统事件（滚动/切换）驱动分析，替代定时轮询（未来优化方向） |
