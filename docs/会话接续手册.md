# Wanna Understanding — 会话接续手册

> **用途**：AI Agent 每次新对话开始时，通过本文档快速了解项目当前状态、近期工作、架构决策和待办事项，避免重复劳动。
>
> **维护者**：所有参与项目的 Agent 和人类维护者，在完成任务后须更新本文档。

---

## §1 项目状态总览（2026-06-19）

| 维度 | 状态 |
|------|:----:|
| **项目阶段** | 🟢 Phase 3 完成（剪贴板读取 + 多模态视觉 + OCR 三重模式） |
| **定位** | 桌面 AI 代码审阅员 — 只读不写，零操作，自动分析编辑器中的代码 |
| **代码获取** | **剪贴板模式**（默认，Ctrl+A→Ctrl+C 读精准代码）→ **OCR 兜底** |
| **文档体系** | ✅ 完整 |
| **Python 工程** | ✅ ruff/pyright/pytest 零错误（61 项） |
| **Git 仓库** | ✅ `develop` 分支，当前 v0.2.3 |

---

## §2 近期完成的工作

### 2026-06-19：剪贴板读取 + 全方向缩放 + OCR 预处理重做

- **新增剪贴板模式**（`screen/clipboard_reader.py`）：通过模拟 Ctrl+A→Ctrl+C 直接
  从编辑器获取精准代码文本，彻底消除 OCR 误差。默认开启，OCR 作为兜底。
  - 按键实现：PowerShell `SendKeys`（COM 路径），兼容 Electron 编辑器（VS Code/Trae/Cursor）
  - `WU_USE_CLIPBOARD=true/false` 开关
- **OCR 预处理管线重做**：去掉二值化（保留灰度层次给 EasyOCR），
  改用 3.0→4.0× 放大 + 锐化 + 对比度增强，`_MAX_OCR_EDGE` 2400→4800
- **OCR 默认语言纯英文**：`["en", "ch_sim"]` → `["en"]`
- **增强后处理**：22 条 Python 符号修复规则；行号过滤 `\d+`→`\d{1,3}[.,;:-]?`
- **心跳分阶段**：区分 OCR / AI / 剪贴板 / 视觉模式提示
- **默认超时 60s→120s**（视觉模式额外 300s）
- **多模态视觉模式**（保留代码）：`WU_USE_VISION=true` 可选，需换用支持 vision 的 API
- **悬浮窗缩放重做**：去掉蓝色三角拖拽角，改为四边+四角全方向缩放（n/s/e/w/ne/nw/se/sw）
- **悬浮窗拖拽修复**：不再依赖 Tkinter 异步 `winfo_width/height`，改为维护自有尺寸缓存
- **`application.py` 拆分**：537→376 行，提取 `AnalysisCoordinator` 混入类
- **文档同步**：`ARCHITECTURE.md`、`技术方案.md`、`错误集.md` 同步更新

### 2026-06-19：OCR 质量改进 + 心跳分阶段 + 超时增大

- **OCR 默认语言改纯英文**：`["en", "ch_sim"]` → `["en"]`
- **增强后处理**：22 条 Python 符号修复规则；行号过滤升级
- **放大提升**：generic profile 2.0→2.5×，`_MAX_OCR_EDGE` 1600→2400
- **二值化提速**：`Image.point()` 替代纯 Python 像素循环（快 100× 以上）
- **心跳区分 OCR/AI 阶段**：`phase` 可变引用
- **默认超时 60s→120s**：缓解 DeepSeek API 间歇性 SSL 握手超时
- **多模态视觉模式**：`WU_USE_VISION=true` 时跳过 OCR，直接发截图给 AI
- **`application.py` 拆分**：537→376 行，提取 `AnalysisCoordinator` 混入类
- **文档同步**：`ARCHITECTURE.md` 补全 41 个文件树；`技术方案.md` 数据流/接口更新

### 2026-06-18：全流程调通 — DPI 感知 + 手动框选 + OCR 中文

- **DPI 感知修复（关键）**：`tcl_bootstrap.py` 添加 `SetProcessDPIAware()`
- **手动框选取代自动检测**：全屏截图 → 用户拖动框选 → Enter 确认
- **OCR 加入中文**：语言列表 `["en"]` → `["en", "ch_sim"]`
- **OCR 置信度降至 0.2**：原 0.3→0.2，多捕获约 30-50% 低置信度但正确的文本
- **OCR 后处理过滤行号**：`_post_process` 过滤纯数字行和单字母行
- **行号裁剪开启**：`trae_dark` / `vscode_dark` / `cursor_dark` 启用 `strip_line_numbers=True`
- **自动冻结延迟 2 秒**：避免流式输出被冻结截断
- **解冻触发重新分析**：解冻时如有窗口变化积压，立即触发分析
- **悬浮窗启动居中**：`geometry` 加上屏幕居中坐标
- **框选界面居中**：修复物理/逻辑像素混用问题
- **选择界面松手不确认**：改为 Enter 确认、Esc 取消

### 2026-06-18：自动冻结 + 中文乱码修复 + 快捷键全面换 Alt

- **自动冻结**：分析完成后自动冻结，冻结期间不截图不分析不调 AI，`Alt+Shift+F` 解冻
- **中文乱码修复**：Consolas 不含中文字形 → 内容区改为 Microsoft YaHei
- **快捷键全部 Alt+Shift**：H 显隐 / J 历史 / O 设置 / F 冻结
- **冻结相关 Bug**：冻结后切窗口不再隐藏；冻结不改内容区、只变标题栏
- **Trae 编辑支持**：新增 `trae_dark` profile
- **OCR 质量改进**：阈值从 50th→25th 百分位，置信度 0.5→0.3
- **修复闪退**：`bad cursor spec "size_s"`——无效光标名导致 tkinter 崩溃
- **托盘菜单加冻结**：右键托盘可直接切换冻结/解冻
- **字体全面调整**：标题 12pt、内容 13pt、设置 12pt（Microsoft YaHei）

### 2026-06-17：日志系统 + 崩溃修复 + 编码头统一

- **新建 `logger.py`**：文件日志 + 自动轮转（1MB×3）+ hook tkinter/threading/sys 未捕获异常
- **集成日志**：`application.py` 主循环全流程日志（状态、分析、异常）
- **修复蓝角缩放**：热区 20→28px，边缘 8→12px；Text 区域不再拦截拖拽事件
- **版本号同步**：`__init__.py` / `pyproject.toml` 对齐 `_version.py`
- **编码头统一**：72 个 `.py` 文件补上 `# -*- coding: utf-8 -*-` 首行声明
- **`application.py` 拆分**（513→445行）：`run_smoke_test` 移至 `smoke.py` 新模块
- **修复 `settings.local.json`**：清理误入的 PowerShell 命令污染

### 2026-06-17：设置热更新 + 技术方案全文同步

- 轮询间隔、托盘开关保存后**立即生效**
- `docs/技术方案.md` Phase 1–3、依赖、风险表与 EasyOCR 对齐

### 2026-06-17：问题修复与文档对齐

- 设置 GUI：框选监控区域、悬浮窗尺寸/透明度、UIA 说明
- `screen/region_picker.py` 供设置与 CLI 复用
- `trim_to_context` 文档明确为「可见区域中间段近似」
- CI 增加 `windows-latest` job；`[ocr]` 装入 Linux CI
- 更新 ADR-0001、技术方案 Phase 3、CONTRIBUTING、错误集、ARCHITECTURE

### 2026-06-17：Phase 3 兼容优化

- **第四批**：系统托盘 `ui/tray.py`（pystray）→ 配置 `WU_TRAY_ENABLED`
- **第三批**：分析历史 `ai/history.py` + 历史对话框 + 设置对话框
- **第二批**：PyCharm/IntelliJ OCR 配置 + UIA 可选层 + 自定义区域
- **第一批**：VS Code/Cursor OCR 配置 + 自适应二值化 + 多显示器 + PyInstaller 打包

### 2026-06-17：流式 AI 输出

- `ai/stream.py` SSE 解析 + `analyze_stream()` 流式 API
- 悬浮窗边生成边刷新，节流间隔可配置

### 2026-06-17：Phase 2 体验优化（第一批）

- 局部代码裁剪 `ai/context.py`（默认 20 行）
- 深色主题自动检测 `ocr/theme.py`
- 悬浮窗智能定位（屏幕边缘自动翻到左侧）
- 全局快捷键 Alt+Shift+H 显示/隐藏
- `python -m wanna_understanding --smoke` 无 GUI 端到端冒烟

### 2026-06-17：MVP 端到端实现

- 实现 `screen/`（capturer、window、region）、`ocr/`（preprocess、engine）
- 实现 `trigger/`（debounce、watcher）、`ai/`（client、prompt、cache）
- 实现 `ui/overlay.py`、`config.py`、`application.py` 编排主循环
- 18 项单元测试覆盖核心逻辑

### 2026-06-17：工程问题清理

- 版本号同步、OCR 引擎改为 EasyOCR、新增 `scripts/ocr_download_models.py`

### 2026-06-17：项目完整初始化

- 项目定位确定、完整文档体系建立、Python 工程配置、社区协作文件

---

## §3 代码获取模式对比

| 模式 | 优先级 | 原理 | 准确率 | 速度 | 依赖 |
|------|:------:|------|:------:|:----:|------|
| **剪贴板** 🆕 | 默认 | `Ctrl+A→Ctrl+C` 读精准代码 | ⭐⭐⭐⭐⭐ | 快 (~1s) | `pywin32` |
| **OCR** | 兜底 | EasyOCR 截图识别 | ⭐⭐ | 慢 (~10s) | `easyocr` |
| **多模态视觉** | 可选 | AI 直接看截图 | ⭐⭐⭐⭐⭐ | 中 | 需 vision API |

---

## §4 架构决策记录

| ADR | 主题 | 状态 |
|-----|------|:----:|
| [ADR-0001](adr/0001-mvp-scope-and-tech-stack.md) | MVP 范围与技术选型 | ✅ 已接受 |
| [ADR-0002](adr/0002-ocr-vs-uia.md) | OCR vs UI Automation 路线 | ✅ 已接受 |

---

## §5 待办事项

### P0：编码 MVP（已完成）

| 任务 | 状态 |
|------|:----:|
| 屏幕捕获 | ✅ |
| OCR 引擎 | ✅ |
| 触发模块 | ✅ |
| AI 客户端 | ✅ |
| 悬浮窗 | ✅ |
| 配置模块 | ✅ |
| 主入口串联 | ✅ |
| 单元测试 | ✅ 61 项 |
| 端到端联调 | ✅ `--smoke` 终端冒烟；GUI 需本机验证 |

### P1：体验优化（已完成）

| 任务 | 状态 |
|------|:----:|
| 深色模式自适应预处理 | ✅ |
| 窗口自动跟随 | ✅ |
| 局部代码发送 | ✅ |
| 流式输出 | ✅ |
| 快捷键 | ✅ Alt+Shift+H/J/O/F |

### P2：基础设施与 Phase 3

| 任务 | 状态 |
|------|:----:|
| 剪贴板读取（默认代码获取方式） | ✅ |
| 剪贴板按键兼容 Electron 编辑器 | ✅ PowerShell SendKeys |
| 全方向缩放（去蓝角） | ✅ |
| application.py 拆分 | ✅ 537→376 行 |
| 测试覆盖 | ✅ 61 项 |
| 多 IDE OCR / 自定义区域 / 历史 / 设置 / 托盘 | ✅ |
| 日志系统 | ✅ |
| 自动冻结 + 快捷键 | ✅ |
| PyInstaller 打包 | ✅ |
| **GUI 实机验收** | 🟡 人工 |
| **发布**（`github_upload_module.py`） | ⬜ 待维护者 |

---

## §6 命令速查

```powershell
# 开发模式安装
pip install -e ".[dev]"

# 运行主程序
python -m wanna_understanding

# 测试
pytest -q                          # 快速
pytest --cov=wanna_understanding    # 覆盖率

# 代码检查
ruff check .                       # Lint
ruff format .                      # 格式化
pyright src/                       # 类型检查

# 查看日志
Select-String -Path "$env:APPDATA\WannaUnderstanding\logs\wanna_understanding.log" -Pattern "剪贴板:|OCR 开始|分析失败" | Select-Object -Last 10
```
