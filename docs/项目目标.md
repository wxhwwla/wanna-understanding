# Wanna Understanding — 项目目标

> 每次对话开始前，必须完整阅读本文档，了解项目的长远愿景和当前焦点。

**最后更新**：2026-06-17

---

## 1. 长远目标

打造一款**只读不写、零操作、跨编辑器**的桌面 AI 代码审阅工具，成为开发者编码时的「第二双眼睛」。

### 1.1 核心理念

```
始终在后台运行           不绑定特定编辑器         只读不写
    │                        │                     │
    ▼                        ▼                     ▼
自动监控屏幕 → OCR 识别代码 → AI 分析 → 悬浮窗展示解释 & 建议
    │                                                   │
    ▼                                                   ▼
零操作，自动跟随滚动/切换                       旁观者，不修改代码
```

### 1.2 目标形态

| 维度 | 目标 |
|------|------|
| 受众 | 所有写代码的人（不限编辑器、不限语言） |
| 内核 | 屏幕捕获 + OCR + AI API 分析 |
| 扩展 | 从 Python MVP → C# 重写 → Rust 极致性能优化 |
| 商业模式 | 免费开源（AGPL-3.0），社区驱动 |

---

## 2. 近期目标

### 2.1 第一阶段：MVP（最小可行产品）

| 模块 | 状态 | 说明 |
|------|:----:|------|
| 项目骨架与规范文档 | ✅ | 已完成 |
| 屏幕捕获 | ⬜ | mss 截图 + win32gui 获取活动窗口 |
| OCR 文字识别 | ⬜ | PaddleOCR 提取代码文本 |
| AI 分析客户端 | ⬜ | DeepSeek API 封装 + Prompt 工程 |
| 悬浮窗展示 | ⬜ | tkinter 无边框置顶窗口 |
| 简单触发 | ⬜ | 定时轮询 + 哈希变化检测 |
| **端到端闭环** | ⬜ | **截图→OCR→AI→展示** |

### 2.2 第二阶段：体验优化

| 模块 | 状态 | 说明 |
|------|:----:|------|
| 防抖调度 | ⬜ | 滚动停止后 0.5 秒再触发 |
| 结果缓存 | ⬜ | 基于代码哈希的 LRU 缓存 |
| 局部代码发送 | ⬜ | 只发送光标附近 10-20 行 |
| 窗口自动跟随 | ⬜ | 悬浮窗跟随编辑器滚动/切换 |

### 2.3 第三阶段：扩展兼容

| 模块 | 状态 | 说明 |
|------|:----:|------|
| VS Code / Cursor 适配 | ⬜ | 深色/浅色主题 OCR 调优 |
| PyCharm 适配 | ⬜ | JetBrains 字体/配色兼容 |
| UI Automation 文本获取 | ⬜ | 作为 OCR 的补充或替代 |
| 自定义监控区域 | ⬜ | 用户框选矩形区域 |

### 2.4 第四阶段：语言迁移

| 模块 | 状态 | 说明 |
|------|:----:|------|
| C# 重写核心 | ⬜ | 更低 CPU 占用 |
| Rust 极致优化 | ⬜ | 截图/OCR 预处理性能最大化 |

---

## 3. 路线图

```
MVP（第1-2周）              优化（第3-4周）           扩展（第5-8周）
────────────────────────────────────────────────────────────────────
屏幕捕获 ✓                   防抖调度 ✓               VS Code 适配
OCR 识别 ✓                   结果缓存 ✓               PyCharm 适配
AI 分析 ✓                   局部发送 ✓               UI Automation
悬浮窗 ✓                    窗口跟随 ✓              自定义区域
端到端闭环 ✓                                           用户配置界面
```

---

## 4. 优先级分解

### P0：MVP 闭环（必须跑通）

| 任务 | 状态 |
|------|:----:|
| 屏幕捕获：mss 截图 + win32gui 活动窗口定位 | ⬜ |
| OCR 引擎：PaddleOCR 封装，图像预处理（放大/二值化/反色） | ⬜ |
| AI 客户端：DeepSeek API 调用 + 结构化 Prompt | ⬜ |
| 悬浮窗：tkinter 无边框置顶，半透明，显示分析结果 | ⬜ |
| 触发机制：定时轮询（5s），哈希变化检测 | ⬜ |
| **端到端联调** | ⬜ |

### P1：体验优化

| 任务 | 状态 |
|------|:----:|
| 防抖调度：滚动停止 0.5s 再触发 | ⬜ |
| LRU 缓存：基于代码哈希缓存 AI 回复 | ⬜ |
| 局部代码：只发送 20 行附近代码给 AI | ⬜ |
| 窗口跟随：自动定位到编辑器旁边 | ⬜ |
| 快捷键：显示/隐藏悬浮窗 | ⬜ |

### P2：兼容与稳定

| 任务 | 状态 |
|------|:----:|
| 深色模式 OCR 准确率优化 | ⬜ |
| 行号/状态栏文字过滤 | ⬜ |
| 多显示器支持 | ⬜ |
| DPI 缩放全面适配 | ⬜ |

### P3：高级功能

| 任务 | 状态 |
|------|:----:|
| UI Automation 替代 OCR（低延迟） | ⬜ |
| 自定义框选监控区域 | ⬜ |
| 主题切换 / 悬浮窗自定义 | ⬜ |
| 分析历史浏览 | ⬜ |

---

## 5. 技术栈

| 组件 | MVP 选型 | 未来迁移 |
|------|----------|----------|
| 编程语言 | Python 3.11+ | C# → Rust |
| 屏幕捕获 | mss + win32gui | Rust 原生 |
| OCR | PaddleOCR | Windows.Media.Ocr (C#) |
| AI API | DeepSeek / OpenAI 兼容 | 通用 API |
| UI | tkinter | WPF / Webview2 |
| 事件监听 | pynput | Windows 钩子 |
| 测试 | pytest | — |
| 代码检查 | ruff + pyright | — |
| 打包 | PyInstaller | .NET / 原生 |
